import React from 'react';
import ReactDOM from 'react-dom/client';
import SpendTracker from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SpendTracker />
  </React.StrictMode>
);
